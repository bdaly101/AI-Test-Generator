import { describe, it, expect{{#if hasBeforeEach}}, beforeEach{{/if}}{{#if hasAfterEach}}, afterEach{{/if}} } from 'vitest';

{{#if mocks}}
{{#each mocks}}
import { vi } from 'vitest';
vi.mock('{{this}}');
{{/each}}
{{/if}}

describe('{{describeName}}', () => {
{{#if hasBeforeEach}}
  beforeEach(() => {
    // TODO: Setup test data
  });
{{/if}}

{{#if hasAfterEach}}
  afterEach(() => {
    // TODO: Cleanup test data
  });
{{/if}}

{{#each tests}}
  it('{{this.name}}', {{#if this.async}}async {{/if}}() => {
    // Arrange
    {{this.arrange}}

    // Act
    {{this.act}}

    // Assert
    {{this.assert}}
  });

{{/each}}
});

